"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {loadClassSettings,saveClassSettings} from "@/lib/onboarding/settings";import {EMPTY_CLASS_SETTINGS} from "@/lib/onboarding/types";import {StepCharacterMessages} from "@/components/onboarding/OnboardingPage";import {WorkspacePage,ws} from "@/components/workspace/WorkspaceUI";
export default function SettingsPage(){const [settings,setSettings]=useState(EMPTY_CLASS_SETTINGS);const [message,setMessage]=useState("");const [ready,setReady]=useState(false);useEffect(()=>{setSettings(loadClassSettings()||EMPTY_CLASS_SETTINGS);setReady(true);},[]);return <WorkspacePage title="설정" description="기관·담당 반과 성품인사를 관리해요."><div className={ws.actions}><Link href="/onboarding/center" className={ws.secondary}>원 정보 수정</Link><Link href="/onboarding/classes" className={ws.secondary}>반·담당 반 수정</Link><Link href="/onboarding/children" className={ws.secondary}>아동 명단 수정</Link></div>{ready&&<section className={ws.card} style={{marginTop:24}}><StepCharacterMessages settings={settings} onChange={p=>setSettings(s=>({...s,...p}))} onPrev={()=>history.back()} onFinish={()=>setMessage(saveClassSettings(settings)?"설정을 저장했어요.":"저장하지 못했어요.")}/><p role="status">{message}</p></section>}</WorkspacePage>;}